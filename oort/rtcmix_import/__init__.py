"""This package is substituted in for RTcmix to allow for testing and code completion in pure Python."""

from commands import *
from instruments import *

if dummy:
    verbose = True
else:
    verbose = False